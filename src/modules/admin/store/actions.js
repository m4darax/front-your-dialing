import { apiDialing } from "@/api/apiDialing"
import axios from "axios"

export const createUser = async ({commit}, userForm) => {

    // try {
    //     const resp = apiDialing.post('/user', userForm);
    //     console.log(resp);
    // } catch (error) {
        
    // }
}